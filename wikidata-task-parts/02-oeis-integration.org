#+TITLE: 02 Oeis Integration
#+DATE: 2026-02-16

> but we can read them from the huggingface ld data no?

⠹ Thinking...
WARNING: Retry #2, retrying within 10.0s..
⠋ Thinking...> You're right! We can query the Wikidata entities from the HuggingFace dataset we already downloaded. Let me create a script to extract number
articles from the existing data.
⠧ Thinking...I'll create the following file: ./eco/extract_number_articles.py (using tool: write)
Purpose: Create script to extract number articles from existing Wikidata batches

+     1: #!/usr/bin/env python3
+     2: """Extract Wikipedia number articles (1-10000) from Wikidata batches"""
+     3: 
+     4: import json
+     5: import os
+     6: from pathlib import Path
+     7: 
+     8: INPUT_DIR = "wikidata_full"
+     9: OUTPUT_DIR = "wikidata_numbers"
+    10: 
+    11: def extract_numbers():
+    12:     """Extract entities for numbers 1-10000"""
+    13:     os.makedirs(OUTPUT_DIR, exist_ok=True)
+    14:     
+    15:     # Target QIDs for numbers (Q + number for most)
+    16:     # Q200 = 200, Q1000 = 1000, etc.
+    17:     target_qids = set(f"Q{n}" for n in range(1, 10001))
+    18:     
+    19:     found = {}
+    20:     total_checked = 0
+    21:     
+    22:     batch_files = sorted(Path(INPUT_DIR).glob("batch_*.jsonl"))
+    23:     
+    24:     print(f"🔍 Searching for number articles (Q1-Q10000)")
+    25:     print(f"   Batches: {len(batch_files)}")
+    26:     print()
+    27:     
+    28:     for batch_file in batch_files:
+    29:         print(f"Processing {batch_file.name}...")
+    30:         
+    31:         with open(batch_file) as f:
+    32:             for line in f:
+    33:                 total_checked += 1
+    34:                 
+    35:                 try:
+    36:                     entity = json.loads(line)
+    37:                     qid = entity.get('id') or entity.get('qid')
+    38:                     
+    39:                     if qid in target_qids:
+    40:                         found[qid] = entity
+    41:                         print(f"  ✓ Found {qid}: {entity.get('label', 'unknown')}")
+    42:                         
+    43:                         # Save individual file
+    44:                         with open(f"{OUTPUT_DIR}/{qid}.json", 'w') as out:
+    45:                             json.dump(entity, out, indent=2)
+    46:                 
+    47:                 except json.JSONDecodeError:
+    48:                     continue
+    49:                 
+    50:                 if total_checked % 100000 == 0:
+    51:                     print(f"  Checked: {total_checked:,} entities, found: {len(found)}")
+    52:     
+    53:     # Save manifest
+    54:     manifest = {
+    55:         "dataset": "wikipedia-numbers",
+    56:         "range": "1-10000",
+    57:         "found": len(found),
+    58:         "entities": sorted(found.keys(), key=lambda x: int(x[1:]))
+    59:     }
+    60:     
+    61:     with open(f"{OUTPUT_DIR}/manifest.json", 'w') as f:
+    62:         json.dump(manifest, f, indent=2)
+    63:     
+    64:     print()
+    65:     print(f"✅ Extraction complete")
+    66:     print(f"   Checked: {total_checked:,} entities")
+    67:     print(f"   Found: {len(found)}/10000 number articles")
+    68:     print(f"   Location: {OUTPUT_DIR}/")
+    69: 
+    70: if __name__ == "__main__":
+    71:     extract_numbers()


Allow this action? Use 't' to trust (always allow) this tool for the session. [y/n/t]:

> we will use https://oeis.org/A104272 as our first index

⠙ Thinking...Fetching content from: https://oeis.org/A104272 [mode: full] (using tool: web_fetch)

Allow this action? Use 't' to trust (always allow) this tool for the session. [y/n/t]:

> y


 ✓ Fetched 21000 bytes (full content) from URL
 - Completed in 0.354s

⠋ Thinking...> Perfect! OEIS A104272 is the Ramanujan primes sequence. The first values are:

2, 11, 17, 29, 41, 47, 59, 67, 71, 97, 101, 107, 127, 149, 151, 167, 179, 181, 227, 229, 233, 239, 241, 263, 269, 281, 307, 311, 347, 349...

Notice 71 is the 9th Ramanujan prime - connecting directly to your 71-layer system! Let me create a script to extract Wikidata entities for 
these Ramanujan primes:
⠇ Thinking...I'll create the following file: ./eco/extract_ramanujan_primes.py (using tool: write)
Purpose: Create script to extract Ramanujan primes from Wikidata

+     1: #!/usr/bin/env python3
+     2: """Extract Wikidata entities for Ramanujan primes (OEIS A104272)"""
+     3: 
+     4: import json
+     5: import os
+     6: from pathlib import Path
+     7: 
+     8: # First 71 Ramanujan primes from OEIS A104272
+     9: RAMANUJAN_PRIMES = [
+    10:     2, 11, 17, 29, 41, 47, 59, 67, 71, 97, 101, 107, 127, 149, 151,
+    11:     167, 179, 181, 227, 229, 233, 239, 241, 263, 269, 281, 307, 311,
+    12:     347, 349, 367, 373, 401, 409, 419, 431, 433, 439, 461, 487, 491,
+    13:     503, 569, 571, 587, 593, 599, 601, 607, 641, 643, 647, 653, 659,
+    14:     661, 677, 683, 691, 709, 719, 727, 739, 751, 757, 769, 787, 797,
+    15:     809, 821, 823, 827
+    16: ]
+    17: 
+    18: INPUT_DIR = "wikidata_full"
+    19: OUTPUT_DIR = "wikidata_ramanujan"
+    20: 
+    21: def extract_ramanujan():
+    22:     os.makedirs(OUTPUT_DIR, exist_ok=True)
+    23:     
+    24:     # Map primes to QIDs (most are Q + number)
+    25:     target_qids = {f"Q{n}": n for n in RAMANUJAN_PRIMES}
+    26:     
+    27:     found = {}
+    28:     total_checked = 0
+    29:     
+    30:     batch_files = sorted(Path(INPUT_DIR).glob("batch_*.jsonl"))
+    31:     
+    32:     print(f"🔢 Extracting Ramanujan Primes (OEIS A104272)")
+    33:     print(f"   Target: {len(RAMANUJAN_PRIMES)} primes")
+    34:     print(f"   Range: {RAMANUJAN_PRIMES[0]} - {RAMANUJAN_PRIMES[-1]}")
+    35:     print(f"   Note: 71 is the 9th Ramanujan prime!")
+    36:     print()
+    37:     
+    38:     for batch_file in batch_files:
+    39:         print(f"Processing {batch_file.name}...")
+    40:         
+    41:         with open(batch_file) as f:
+    42:             for line in f:
+    43:                 total_checked += 1
+    44:                 
+    45:                 try:
+    46:                     entity = json.loads(line)
+    47:                     qid = entity.get('id') or entity.get('qid')
+    48:                     
+    49:                     if qid in target_qids:
+    50:                         prime = target_qids[qid]
+    51:                         found[qid] = entity
+    52:                         
+    53:                         # Check if it's 71
+    54:                         marker = " ⭐ OMEGA PRIME" if prime == 71 else ""
+    55:                         print(f"  ✓ Found {qid} ({prime}){marker}")
+    56:                         
+    57:                         # Save individual file
+    58:                         with open(f"{OUTPUT_DIR}/{qid}.json", 'w') as out:
+    59:                             json.dump(entity, out, indent=2)
+    60:                 
+    61:                 except json.JSONDecodeError:
+    62:                     continue
+    63:                 
+    64:                 if total_checked % 100000 == 0:
+    65:                     print(f"  Checked: {total_checked:,}, found: {len(found)}/{len(RAMANUJAN_PRIMES)}")
+    66:     
+    67:     # Create manifest
+    68:     found_primes = sorted([target_qids[qid] for qid in found.keys()])
+    69:     
+    70:     manifest = {
+    71:         "dataset": "ramanujan-primes",
+    72:         "oeis": "A104272",
+    73:         "description": "Ramanujan primes R_n: smallest number such that pi(x) - pi(x/2) >= n",
+    74:         "total": len(RAMANUJAN_PRIMES),
+    75:         "found": len(found),
+    76:         "primes": found_primes,
+    77:         "omega_prime": 71,
+    78:         "omega_position": 9
+    79:     }
+    80:     
+    81:     with open(f"{OUTPUT_DIR}/manifest.json", 'w') as f:
+    82:         json.dump(manifest, f, indent=2)
+    83:     
+    84:     print()
+    85:     print(f"✅ Extraction complete")
+    86:     print(f"   Checked: {total_checked:,} entities")
+    87:     print(f"   Found: {len(found)}/{len(RAMANUJAN_PRIMES)} Ramanujan primes")
+    88:     print(f"   71 (Omega prime): {'FOUND' if 'Q71' in found else 'NOT FOUND'}")
+    89:     print(f"   Location: {OUTPUT_DIR}/")
+    90: 
+    91: if __name__ == "__main__":
+    92:     extract_ramanujan()


Allow this action? Use 't' to trust (always allow) this tool for the session. [y/n/t]:

> y

Creating: ./eco/extract_ramanujan_primes.py
 - Completed in 0.0s

⠸ Thinking...> Created! This script will extract the first 71 Ramanujan primes from your existing Wikidata batches. The sequence A104272 is perfect because:

1. 71 is the 9th Ramanujan prime - connecting to your 71-layer system
2. Ramanujan primes are defined by π(x) - π(x/2) ≥ n - a density property
3. They generalize Bertrand's postulate
4. They're named after Ramanujan, connecting to modular forms and the Monster group

The script will search through your 30GB of Wikidata and extract entities Q2, Q11, Q17, Q29, Q41, Q47, Q59, Q67, Q71, etc.

Want me to run it on your existing wikidata_full batches?
 ▸ Time: 47s

